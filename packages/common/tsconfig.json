{
  "compilerOptions": {
    "target": "es2016", // 호환성을 위한 적절한 JS 버전
    "module": "commonjs", // Node.js 및 대부분의 빌드 시스템이 이해하는 모듈 방식
    "strict": true, // 1인 개발자의 실수를 막아줄 가장 강력한 안전장치
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    /* --- '인쇄소'의 핵심 설정 --- */
    "outDir": "dist", // "인쇄물(JS)"이 나올 폴더
    "rootDir": "src", // "원본 원고(TS)"가 있는 폴더
    "declaration": true, // "설계도 사본(.d.ts)"도 함께 인쇄! (가장 중요)
    "sourceMap": true // 디버깅을 위한 '페이지 번호' 맵
  },
  "include": [
    "src" // '인쇄'할 대상은 'src' 폴더 내부로 한정
  ],
  "exclude": [
    "node_modules", // '인쇄'할 필요 없는 기계 부품들
    "dist" // '인쇄물'을 다시 '인쇄'하지 않도록 방지
  ]
}
